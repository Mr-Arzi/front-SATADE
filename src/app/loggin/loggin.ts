import { Component } from '@angular/core';
import { RouterLink } from "@angular/router";

@Component({
  selector: 'app-loggin',
  imports: [RouterLink],
  templateUrl: './loggin.html',
  styleUrl: './loggin.css',
})
export class Loggin {

}
